[package]
name = "ghtest"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.5", features = ["serde"] }
[dev-dependencies]
chrono = { version = "0.4.39", features = ["serde"] }
criterion = "0"

[[bench]]
name = "timestamp"
harness = false
