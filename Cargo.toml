[package]
name = "ghtest"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.5", features = ["serde"] }
[dev-dependencies]
criterion = "0"

[[bench]]
name = "_template"
harness = false
